# Workarounds tracker — runtime cache, regenerated on demand by forge-workarounds-check.sh
learnings/.workarounds-tracker.json

# Build output — regenerated by bin/forge-build from core/
build/

# Forge's own deployed skills (regenerated by build for dogfooding)
.agents/
