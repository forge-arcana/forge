# Workarounds tracker — runtime cache, regenerated on demand by forge-workarounds-check.sh
learnings/.workarounds-tracker.json

# Build output — regenerated by bin/forge-build from core/ + adapter manifest
build/

# Workspace deploy targets — regenerated by `bin/forge-build <adapter> --deploy`.
# Bob commands and Antigravity workflows live here when deployed for local use.
.bob/
.agent/
.agents/
